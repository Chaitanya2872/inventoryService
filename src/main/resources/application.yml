server:
  port: 8082

spring:
  application:
    name: INVENTORY-SERVICE  # FIXED: Changed to uppercase to match gateway route

  datasource:
    url: jdbc:postgresql://45.79.122.134:5432/inventoryservice_db?options=-c%20TimeZone=Asia/Kolkata
    username: jack2872
    password: Tester@4723$
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 30000
      connection-timeout: 30000
      max-lifetime: 1800000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# FIXED: Eureka configuration
eureka:
  client:
    service-url:
      defaultZone: http://45.79.121.181:8761/eureka/  # FIXED: Use actual server IP
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true  # FIXED: Use IP instead of hostname
    instance-id: ${spring.application.name}:${random.value}
    # REMOVED hostname: localhost - let Eureka auto-detect

jwt:
  secret: mySecretKeyForJWTShouldBeAtLeast256BitsLongForHS256Algorithm1234567890
  expiration: 86400000

logging:
  level:
    root: INFO
    com.bmsedge.inventory: DEBUG
    org.springframework.security: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always